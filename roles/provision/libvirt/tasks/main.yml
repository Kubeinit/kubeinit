---
# Copyright pystol.org
# All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License"); you may
# not use this file except in compliance with the License. You may obtain
# a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.


# "pystol" will search for and load any operating system variable file

# found within the "vars/" path. If no OS files are found the task will skip.

##
## Hypervisor installing dependencies and rebooting.
##

- fail:
    msg: The hypervisor machine needs to be CentOS 8.x
  when: not (ansible_distribution == 'CentOS' and ansible_distribution_version|int == 8)
  tags:
    - provision_libvirt

- name: install requirements
  package:
    name: "{{ item }}"
    state: present
  with_items: "{{ pystol_provision_hypervisor_dependencies }}"
  tags:
    - provision_libvirt

- name: upgrade all packages
  yum:
    name: '*'
    state: latest
  tags:
    - provision_libvirt

- name: check if check restarted file exists
  stat:
    path: "{{ pystol_provision_hypervisor_tmp_dir }}/restarted"
  register: provision_restared_hypervisor
  tags:
    - provision_libvirt

- name: Reboot host and wait for it to restart
  reboot:
    msg: "Reboot initiated by Ansible"
    connect_timeout: 5
    reboot_timeout: 600
    pre_reboot_delay: 0
    post_reboot_delay: 30
    test_command: whoami
  when: provision_restared_hypervisor.stat.exists == false
  tags:
    - provision_libvirt

- name: ensure check file exists
  copy:
    content: ""
    dest: "{{ pystol_provision_hypervisor_tmp_dir }}/restarted"
    force: no
    group: sys
    owner: root
    mode: 0555
  tags:
    - provision_libvirt

##
## Hypervisor installing users and permissions and other bits that do not require reboot.
##

- name: check if base image exists
  stat:
    path: "{{ pystol_provision_target_image_dir }}/{{ pystol_provision_source_image_file }}"
  register: vm_image
  tags:
    - provision_libvirt

- name: push base vm image to hypervisor
  get_url:
    url: "{{ pystol_provision_source_image_dir }}/{{ pystol_provision_source_image_file }}"
    dest: "{{ pystol_provision_target_image_dir }}/{{ pystol_provision_source_image_file }}"
  when: vm_image.stat.exists == false
  tags:
    - provision_libvirt

- name: Change image permissions
  file:
    path: "{{ pystol_provision_target_image_dir }}/{{ pystol_provision_source_image_file }}"
    owner: qemu
    group: qemu
    mode: u=rw,g=r,o=r
  tags:
    - provision_libvirt

- name: "Make sure we have a '{{ pystol_provision_cloud_user }}' group"
  group:
    name: "{{ pystol_provision_cloud_user }}"
    state: present
  tags:
    - provision_libvirt

- name: "Allow '{{ pystol_provision_cloud_user }}' group to have passwordless sudo"
  lineinfile:
    dest: /etc/sudoers
    state: present
    regexp: "^%{{ pystol_provision_cloud_user }}"
    line: "%{{ pystol_provision_cloud_user }} ALL=(ALL) NOPASSWD: ALL"
    validate: 'visudo -cf %s'
  tags:
    - provision_libvirt

- name: "Add sudoers users to {{ pystol_provision_cloud_user }} group"
  user: "name={{ pystol_provision_cloud_user }} groups={{ pystol_provision_cloud_user }} append=yes state=present createhome=yes"
  tags:
    - provision_libvirt

- name: Add Users to libvirt
  action: user name="{{ item }}" groups="kvm,libvirt" append=yes
  with_items:
    - "{{ pystol_provision_cloud_user }}"
    - root
  tags:
    - provision_libvirt

- name: check if /usr/bin/qemu-kvm exists
  stat:
    path: /usr/bin/qemu-kvm
  register: qemu_kvm
  tags:
    - provision_libvirt

- name: Create symbolic link for qemu
  file:
    src: /usr/libexec/qemu-kvm
    dest: /usr/bin/qemu-kvm
    state: link
  when: qemu_kvm.stat.exists == false
  tags:
    - provision_libvirt

- name: Create ssh folder
  file:
    path: "/home/{{ pystol_provision_cloud_user }}/.ssh/"
    state: directory
  tags:
    - provision_libvirt

- name: Generate an OpenSSH keypair with the default values (4096 bits, rsa)
  openssh_keypair:
    path: "/root/.ssh/id_rsa"
    owner: root
    group: root
  tags:
    - provision_libvirt

- name: Generate an OpenSSH keypair with the default values (4096 bits, rsa)
  openssh_keypair:
    path: "/home/{{ pystol_provision_cloud_user }}/.ssh/id_rsa"
    owner: "{{ pystol_provision_cloud_user }}"
    group: "{{ pystol_provision_cloud_user }}"
  tags:
    - provision_libvirt

- name: Fix owner of the generated pub key
  file:
    path: "/home/{{ pystol_provision_cloud_user }}/.ssh/id_rsa.pub"
    owner: "{{ pystol_provision_cloud_user }}"
    group: "{{ pystol_provision_cloud_user }}"
  tags:
    - provision_libvirt

- name: Insert the default libvirt uri in user's home
  lineinfile:
    path: "/home/{{ pystol_provision_cloud_user }}/.bashrc"
    line: export LIBVIRT_DEFAULT_URI=qemu:///system
  tags:
    - provision_libvirt

##
## Destroy all VMs and networks
##

- name: Destroy vms
  virt:
      name: "{{ item.name }}"
      command: destroy
  with_items: "{{ pystol_provision_cluster_hosts }}"
  ignore_errors: true
  tags:
    - provision_libvirt

- name: Undefine vms
  virt:
      name: "{{ item.name }}"
      command: undefine
  with_items: "{{ pystol_provision_cluster_hosts }}"
  ignore_errors: true
  tags:
    - provision_libvirt

- name: Destroy pystol networks
  virt_net:
    command: destroy
    name: "{{ item.name }}"
  with_items: "{{ pystol_provision_cluster_nets }}"
  ignore_errors: true
  tags:
    - provision_libvirt

##
## VMs definition, creation and configuration.
##

- name: Create vm backing store from base vm image
  copy: remote_src=True
    src="{{ pystol_provision_target_image_dir }}/{{ pystol_provision_source_image_file }}"
    dest="{{ pystol_provision_target_image_dir }}/{{ item.name }}.qcow2"
    force=yes  # Do not recopy if it has been modified
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- name: Ensure the backing store is large enough
  command: "qemu-img resize {{ pystol_provision_target_image_dir }}/{{ item.name }}.qcow2 +{{ item.disk }}"
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- name: Grow the partition
  command: "virt-resize  --expand /dev/sda1 {{ pystol_provision_target_image_dir }}/{{ pystol_provision_source_image_file }} {{ pystol_provision_target_image_dir }}/{{ item.name }}.qcow2"
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

##
## VM and networks definition, creation and configuration.
##

- name: Create directories for networking interfaces per node
  file:
    path: "{{ pystol_provision_hypervisor_tmp_dir }}/{{ item.name }}"
    state: directory
    mode: 0775
    recurse: yes
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

# We can render any cloudinit parameter we want/need here
- name: Create the network interface template for networking details
  template:
    src: "pystol-ifcfg-eth0.j2"
    dest: '{{ pystol_provision_hypervisor_tmp_dir }}/{{ item.name }}/ifcfg-eth0'
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- name: Create the hosts template
  template:
    src: "101-cloudinit-hosts.tmpl.j2"
    dest: '{{ pystol_provision_hypervisor_tmp_dir }}/{{ item.name }}/hosts.redhat.tmpl'
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- name: Create the network cloudinit templates for networking details
  template:
    src: "102-cloudinit-cloud.cfg.j2"
    dest: '{{ pystol_provision_hypervisor_tmp_dir }}/{{ item.name }}/cloud.cfg'
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- name: "Inject {{ pystol_provision_cloud_user }} virt-customize assets"
  shell: |
    virt-customize -a {{ pystol_provision_target_image_dir }}/{{ item.name }}.qcow2 \
      --run-command  'id -u {{ pystol_provision_cloud_user }} &>/dev/null || /usr/sbin/useradd -u 1000 {{ pystol_provision_cloud_user }}' \
      --ssh-inject {{ pystol_provision_cloud_user }}:file:{{ pystol_provision_source_keystore_dir }}/{{ pystol_provision_source_pubkey_file }} \
      --ssh-inject root:file:/root/.ssh/id_rsa.pub \
      --ssh-inject root:file:/root/.ssh/authorized_keys \
      --hostname {{ item.name }}.{{ pystol_provision_cluster_domain }} \
      --copy-in {{ pystol_provision_hypervisor_tmp_dir }}/{{ item.name }}/ifcfg-eth0:/etc/sysconfig/network-scripts \
      --copy-in {{ pystol_provision_hypervisor_tmp_dir }}/{{ item.name }}/hosts.redhat.tmpl:/etc/cloud/templates \
      --copy-in {{ pystol_provision_hypervisor_tmp_dir }}/{{ item.name }}/cloud.cfg:/etc/cloud \
      --selinux-relabel --root-password password:{{ pystol_provision_vms_default_password }}
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- name: define vm
  virt:
      name: "{{ item.name }}"
      command: define
      xml: "{{ lookup('template', 'vm.xml.j2') }}"
      uri:  qemu:///session
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- name: define pystol networks
  virt_net:
    command: define
    name: "{{ item.name }}"
    xml: '{{ lookup("template", "pystol-nets.xml.j2") }}'
  with_items: "{{ pystol_provision_cluster_nets }}"
  tags:
    - provision_libvirt

- name: Activate pystol networks
  virt_net:
    state: active
    name: "{{ item.name }}"
  with_items: "{{ pystol_provision_cluster_nets }}"
  tags:
    - provision_libvirt

- name: Autostart pystol networks
  virt_net:
    autostart: yes
    name: "{{ item.name }}"
  with_items: "{{ pystol_provision_cluster_nets }}"
  tags:
    - provision_libvirt

- name: Create the interface template
  template:
    src: "pystol-interface.xml.j2"
    dest: '{{ pystol_provision_hypervisor_tmp_dir }}/{{ item.cluster_host }}/pystol-interface.xml'
  with_items: "{{ pystol_provision_cluster_attach_networks }}"
  tags:
    - provision_libvirt

- name:  connect the network interfaces
  shell: |
    virsh attach-device \
      {{ item.cluster_host }} \
      {{ pystol_provision_hypervisor_tmp_dir }}/{{ item.cluster_host }}/pystol-interface.xml \
      --config
  with_items: "{{ pystol_provision_cluster_attach_networks }}"
  tags:
    - provision_libvirt

- name: Pause execution for 1 minute
  pause:
    minutes: 1
  tags:
    - provision_libvirt

##
## Run all created resources.
##

- name: Start the VMs
  virt:
    name: "{{ item.name }}"
    uri:  qemu:///session
    state: running
  with_items: "{{ pystol_provision_cluster_hosts }}"
  loop_control:
    pause: 30
  tags:
    - provision_libvirt

- name: set autostart for a VM
  virt:
    name: "{{ item.name }}"
    uri:  qemu:///session
    autostart: no
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

# - name:  connect the network interfaces
#   shell: |
#     virsh attach-interface \
#       --domain {{ item.cluster_host }} \
#       --type network \
#       --source {{ item.net }} \
#       --model virtio \
#       --config \
#       --live
#   with_items: "{{ pystol_provision_cluster_attach_networks }}"
#   tags:
#     - provision_libvirt

# - name: Pause execution for 5 minutes
#   pause:
#     minutes: 5
#   tags:
#     - provision_libvirt

# # These rules are not needed....
# - name: "Allow guests to have Internet access"
#   shell: |
#     iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
#     iptables -A OUTPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
#     iptables -I INPUT 1 -s 0.0.0.0/0 -d 0.0.0.0/0 -j ACCEPT
#     iptables -I OUTPUT 1 -s 0.0.0.0/0 -d 0.0.0.0/0 -j ACCEPT
#
#     # default_iface is the network interface having access to the internet
#     default_iface=$(awk '$2 == 00000000 { print $1 }' /proc/net/route)
#     iptables -t nat -A POSTROUTING -o envmw -j MASQUERADE
#     iptables -I FORWARD 1 -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
#     iptables -I FORWARD 1 -i virbr1 -o $default_iface -j ACCEPT
#
#     # watch -n0.1 iptables -L -n -v --line-numbers
#     # iptables -D INPUT 11
#     # iptables-save > iptables_dump
#     # iptables-restore < iptables_dump
#     service iptables save
#   with_items: "{{ pystol_provision_cluster_nets }}"
#   tags:
#     - provision_libvirt

##
## Cleanup.
##

- name: check if /root/.ssh/known_hosts exists
  stat:
    path: /root/.ssh/known_hosts
  register: known_hosts_file
  tags:
    - provision_libvirt

- name:  Reset ssh keys in hypervisor
  shell: |
    ssh-keygen -R  {{ item.name }}
    ssh-keygen -R  {{ item.name }}.{{ pystol_provision_cluster_domain }}
    ssh-keygen -R  {{ item.eth0.ip }}
  with_items: "{{ pystol_provision_cluster_hosts }}"
  when: known_hosts_file.stat.exists == true
  tags:
    - provision_libvirt

- name: reset ssh keys for hostname
  become: no
  delegate_to: localhost
  command: "ssh-keygen -R  {{ item.name }}"
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- name: reset ssh keys for FQDN
  become: no
  delegate_to: localhost
  command: "ssh-keygen -R  {{ item.name }}.{{ pystol_provision_cluster_domain }}"
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- name: reset ssh keys for ip address
  become: no
  delegate_to: localhost
  command: "ssh-keygen -R  {{ item.eth0.ip }}"
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt

- ping:
  tags:
    - provision_libvirt

- name: Pause execution for 3 minutes
  pause:
    minutes: 2
  tags:
    - provision_libvirt

- name: "Make sure there are no zombie guests"
  shell: |
    #!/bin/bash
    host={{ item.eth0.ip }}
    ping -c 5 -w 5 $host
    if [ ! $? -eq 0 ]; then
      echo "Cant reach the host, need to restart to refresh the network"
      virsh reboot {{ item.name }}
    fi
  with_items: "{{ pystol_provision_cluster_hosts }}"
  tags:
    - provision_libvirt
    - provision_libvirt_restart_zombies



# - name: check if check restarte2d file exists
#   stat:
#     path: "{{ pystol_provision_hypervisor_tmp_dir }}/restarted2"
#   register: provision_restared2_hypervisor
#   tags:
#     - provision_libvirt
#
# - name: Reboot host and wait for it to restart
#   reboot:
#     msg: "Reboot initiated by Ansible"
#     connect_timeout: 5
#     reboot_timeout: 600
#     pre_reboot_delay: 0
#     post_reboot_delay: 30
#     test_command: whoami
#   when: provision_restared2_hypervisor.stat.exists == false
#   tags:
#     - provision_libvirt
#
# - name: ensure check file exists
#   copy:
#     content: ""
#     dest: "{{ pystol_provision_hypervisor_tmp_dir }}/restarted2"
#     force: no
#     group: sys
#     owner: root
#     mode: 0555
#   tags:
#     - provision_libvirt
#
