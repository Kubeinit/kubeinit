---
- name: Disabling Swap on all nodes
  shell: swapoff -a
  tags:
    - kubernetes_common

- name: Commenting Swap entries in /etc/fstab
  replace:
    path: /etc/fstab
    regexp: '(.*swap*)'
    replace: '#\1'
  tags:
    - kubernetes_common
